import React from 'react';
import SectionPlaceholder from '../../components/admin/SectionPlaceholder';

const Services: React.FC = () => {
  return (
    <SectionPlaceholder
      title="Services"
      description="Service management hooks into the new admin layout, but the CRUD screen itself has been left for a dedicated pass."
    />
  );
};

export default Services;
