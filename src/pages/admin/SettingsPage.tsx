import React from 'react';
import SectionPlaceholder from '../../components/admin/SectionPlaceholder';

const SettingsPage: React.FC = () => {
  return (
    <SectionPlaceholder
      title="Settings"
      description="Use this section for admin profile, site config, and security controls once the rest of the console is in place."
    />
  );
};

export default SettingsPage;
